# Backend de lecture depuis le 2026-08-22 : le miroir Drive (adr/0012, invariant 7).
# ProtectSystem=strict le rend deja non inscriptible ; le declarer explicitement
# documente l intention et empeche qu un ReadWritePaths ajoute plus tard n ouvre
# en ecriture la source de verite du RAG depuis le seul service expose a internet.
[Service]
ReadOnlyPaths=/srv/vault-mirror
