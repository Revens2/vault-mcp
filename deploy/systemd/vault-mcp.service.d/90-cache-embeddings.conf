# Meme cache d embeddings que le full et l incremental : une requete deja vue
# (ou un fragment) ne repasse pas par le modele.
[Service]
Environment=VAULT_MCP_EMBED_CACHE=/opt/vault-mcp/index/embed_cache.sqlite
