# Dépendances DIRECTES de vault-mcp (imports réels de vault_mcp/ et scripts/, relevés le 2026-09-10).
# Le lockfile à hashes `requirements.lock` est compilé depuis ce fichier, contraint au venv de
# production (arm64, Python 3.12) : `pip-compile --generate-hashes -c constraints-live.txt`.
# `tiktoken` volontairement ABSENT : import paresseux et optionnel (wiki_jobs.estimate_tokens_local,
# repli documenté octets/2.44) — l'ajouter changerait le comptage de tokens du Wiki ingest.
mcp==2.2.0
uvicorn==0.52.1
starlette==1.3.1
pydantic==2.13.4
httpx==0.28.1
numpy==2.5.2
fastembed==0.8.0
